{
  "scripts": {
    "start": "node dist/server.js",
    "build": "npm run clean && tsc && npm run copyFiles",
    "clean": "rm -r dist",
    "copyFiles": "cp src/public/*.html src/public/*.css dist/public"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.7.4",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
